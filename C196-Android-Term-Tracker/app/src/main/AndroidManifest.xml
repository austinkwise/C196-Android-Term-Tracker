<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.termtracker_c196">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <receiver
            android:name=".Helper.MyReceiver"
            android:enabled="true"
            android:exported="true"></receiver>

        <activity android:name=".Activity.AddNotesActivity" />
        <activity android:name=".Activity.NotesActivity" />
        <activity android:name=".Activity.AddAssessmentActivity" />
        <activity android:name=".Activity.AssessmentActivity" />
        <activity android:name=".Activity.CourseActivity" />
        <activity android:name=".Activity.AddCourseActivity" />
        <activity android:name=".Activity.AddMentorActivity" />
        <activity android:name=".Activity.MentorActivity" />
        <activity android:name=".Activity.AddTermActivity" />
        <activity android:name=".Activity.TermsActivity" />
        <activity android:name=".Activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>